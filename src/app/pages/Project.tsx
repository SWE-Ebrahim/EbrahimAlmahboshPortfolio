// This file has been moved to /frontend/src/app/project/[id]/page.tsx
// This is a placeholder to avoid conflicts
import { redirect } from 'next/navigation';

export default function Project() {
  // Redirect to the new location
  redirect('/projects');
}